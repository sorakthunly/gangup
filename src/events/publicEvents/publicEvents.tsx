import React from 'react';
import { Text } from 'react-native-ui-kitten';

import BaseLayout from '../../baseLayout/BaseLayout';

const PublicEvent = () => {
    return (
        <BaseLayout>
            <Text>This is My Public Events page!!!!</Text>
        </BaseLayout>
    );
};

export default PublicEvent;
